# cyclescope

A lightweight C++ scope and function tracing library with pluggable backends and a Perfetto compatible trace event emitter.

The initial backend uses compiler instrumentation via -finstrument-functions.

Status: Phase 1, scaffolding. No releases yet.
